master:
    script:
        - rm ~/.parse/edu.kit.ipd.parse.shallownlp.ShallowNLP.conf
        - mvn source:jar deploy -B
    tags:
        - mvn
    only:
        - master

branches:
    script:
        - mvn test -B
    tags:
        - mvn
    only:
        - branches
    except:
        - master
