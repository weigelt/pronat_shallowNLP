master:
    script:
        - mvn test -B
    tags:
        - mvn
    only:
        - master
    except:
        - branches

branches:
    script:
        - mvn test -B
    tags:
        - mvn
    only:
        - branches
    except:
        - master
